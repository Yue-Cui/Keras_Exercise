Originally Lynda HW practice, models are built on top of each other, so repetition exists.

My venv:
pip install -U scikit-learn 
pip install pandas 
pip install keras
pip install h5py
pip install pillow
